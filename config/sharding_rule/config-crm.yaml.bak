schemaName: crm
dataSources:
  ds0:
    url: jdbc:mysql://10.159.0.102:3308/crm_0001?serverTimezone=UTC&useSSL=false
    username: crm
    password: 1jian8Shu)
    connectionTimeoutMilliseconds: 30000
    idleTimeoutMilliseconds: 60000
    maxLifetimeMilliseconds: 1800000
    maxPoolSize: 50
  ds1:
    url: jdbc:mysql://10.159.0.104:3308/crm_0002?serverTimezone=UTC&useSSL=false
    username: crm
    password: 1jian8Shu)
    connectionTimeoutMilliseconds: 30000
    idleTimeoutMilliseconds: 60000
    maxLifetimeMilliseconds: 1800000
    maxPoolSize: 50
  ds2:
    url: jdbc:mysql://10.159.0.106:3308/crm_0003?serverTimezone=UTC&useSSL=false
    username: crm
    password: 1jian8Shu)
    connectionTimeoutMilliseconds: 30000
    idleTimeoutMilliseconds: 60000
    maxLifetimeMilliseconds: 1800000
    maxPoolSize: 50
  ds3:
    url: jdbc:mysql://10.159.0.108:3308/crm_0004?serverTimezone=UTC&useSSL=false
    username: crm
    password: 1jian8Shu)
    connectionTimeoutMilliseconds: 30000
    idleTimeoutMilliseconds: 60000
    maxLifetimeMilliseconds: 1800000
    maxPoolSize: 50


shardingRule:
  bindingTables:
  - AGREEMENT_ATTR_ORDER,AGREEMENT_OBJ_ORDER,AGREEMENT_ORDER,BAL_SHARE_ITEM_ORDER,BAL_SHARE_ORDER,BC_MEMBER_ATTR_ORDER,BC_MEMBER_ORDER,BC_ROLE_ORDER,BFM_PROCESS_BO,BILL_SPLIT_ORDER,CART_ITEM,CART_ITEM_REL,CDR_PRINT_ORDER,CREDIT_BALANCE_ORDER,CREDIT_LIMIT_ORDER,CUST_CONTACT,CUST_CONTRACT_ITEM_SUBS_ORDER,CUST_ORDER,CUST_ORDER_ATTACH,CUST_ORDER_ATTR,CUST_ORDER_VER,CUST_ORDER_VER_ITEM,CUST_PROF_ORDER,DELIVERY_ORDER,DELIVERY_ORDER_GOODS_ITEM,DELIVERY_ORDER_ITEM,DP_OFFER_ORDER,DP_OFFER_ORDER_ATTR,FELLOW_NBR_ORDER,GOODS_ORDER,GOODS_ORDER_ATTR,GOODS_SALE_ORDER,GOODS_SALE_ORDER_ATTR,GOODS_SALE_ORDER_COUPON,GOODS_SALE_ORDER_DETAIL,GOODS_SALE_ORDER_REL,GOODS_SALE_ORDER_RES,HOME_CELL_ORDER,HOME_ZONE_ORDER,INDEP_PROD_ORDER_ATTR,INDIVIDUAL_LIFECYCLE_ORDER,OFFER_PROD_RELA_ORDER,ORDER_ITEM,ORDER_ITEM_ATTR,ORDER_ITEM_KPI,ORDER_ITEM_MEMO,ORDER_OTC_INST,ORDER_OTC_INST_PRICE,ORDER_OTC_INST_TAX,ORDER_OTC_INSTALLMENT_PLAN,ORDER_OTC_IP_DETAIL,ORDER_OTC_QRY_BENEFIT,ORDER_OTC_QRY_DEPOSIT,ORDER_OTC_QRY_ITEM,ORDER_OTC_QRY_PRICE,ORDER_OTC_QRY_PRICE_ADJUST,ORDER_OTC_QRY_TAX,ORDER_OTC_TRANSACTION,ORDER_RELA,PARALLEL_UPGRADE_ORDER,PROMO_OFFER_ORDER,RES_ORDER,SHOPPING_CART,SIM_CARD_INFO_ORDER,STAFF_DRAFT,SUBS_BASE_ORDER,SUBS_BIND_ORDER,SUBS_CONTACT_MAN_ORDER,SUBS_RELA_ATTR_ORDER,SUBS_SOCIAL_ORDER,TIMER_EVENT,TIMER_EVENT_MEMO,WF_DISPATCH_ORDER,WF_WORK_ORDER,WF_WORK_ORDER_ACTION,WF_WORK_ORDER_ATTR,WF_WORK_ORDER_HIS
  broadcastTables:
  - AGREEMENT
  - AGREEMENT_ACTION_LIMIT
  - AGREEMENT_APPLY
  - AGREEMENT_GROUP
  - AGREEMENT_GROUP_MEMBER
  - AGREEMENT_INSTALLMENT
  - AGREEMENT_LEVEL_ACTION
  - AGREEMENT_MAINTAINER
  - AGREEMENT_MAINTAINER_OBJ
  - AGREEMENT_NOTIFY
  - AGREEMENT_OBJ
  - AGREEMENT_OBJ_TYPE
  - AGREEMENT_OBJ_TYPE_ACTION
  - AGREEMENT_PENALTY
  - AGREEMENT_PENALTY_TPL
  - AGREEMENT_PRICE
  - AGREEMENT_QUANTITY_LIMIT
  - AGREEMENT_RELA
  - AGREEMENT_RELA_TYPE
  - AGREEMENT_RSTR
  - AGREEMENT_RULE
  - AGREEMENT_TPL
  - AGREEMENT_TYPE
  - AGREEMENT_TYPE_TAG
  - AGREEMENT_VER
  - ALL_SERV_TYPE
  - ATTACH_TYPE
  - ATTR
  - ATTR_APPLY_CATG
  - ATTR_APPLY_CHANNEL
  - ATTR_CATG
  - ATTR_DRIVER
  - ATTR_GROUP
  - ATTR_GROUP_MEM
  - ATTR_POPEDIT
  - ATTR_TYPE
  - ATTR_VALUE
  - ATTR_VALUE_LINKAGE
  - BASE_ATTR
  - BFM_AD_CONFIG
  - BFM_APP
  - BFM_AREA
  - BFM_BULLETIN
  - BFM_BULLETIN_LEVEL
  - BFM_BULLETIN_RECIPIENT
  - BFM_BULLETIN_TMPL
  - BFM_BULLETIN_TYPE
  - BFM_BULLETIN_VIEW
  - BFM_COMPONENT_PRIV
  - BFM_DATA_PRIV
  - BFM_DIR
  - BFM_EVENT_CODE
  - BFM_EVENT_SRC
  - BFM_EVENT_TYPE
  - BFM_GROUP_DATA_PRIV
  - BFM_JOB
  - BFM_JOB_LAYOUT
  - BFM_JOB_ROLE
  - BFM_JOB_USER_GROUP
  - BFM_JOB_WORK_TIME
  - BFM_LAYOUT_DEF
  - BFM_LAYOUT_DEF_HIS
  - BFM_MENU
  - BFM_MENU_DIR
  - BFM_MENU_FAVORITE
  - BFM_MODULE
  - BFM_MVNO_HIS
  - BFM_ORG
  - BFM_ORG_JOB
  - BFM_ORG_TYPE
  - BFM_ORG_WORK_TIME
  - BFM_PARAMS
  - BFM_PART_SP
  - BFM_PORTAL
  - BFM_PORTAL_BIZ_PARAM
  - BFM_PORTAL_CHANNEL
  - BFM_PORTAL_INDEX
  - BFM_PORTAL_MENU
  - BFM_PORTAL_SP
  - BFM_PORTLET_SCOPE
  - BFM_PORTLET_TYPE
  - BFM_PORTLETS
  - BFM_PRIV
  - BFM_PRIV_TYPE
  - BFM_PROCESS_ACTIVITY
  - BFM_PROCESS_STATE
  - BFM_ROLE
  - BFM_ROLE_DATA_PRIV
  - BFM_ROLE_PORTAL
  - BFM_ROLE_PRIV
  - BFM_SECURITY_RULE
  - BFM_SERV_LOG
  - BFM_STAFF
  - BFM_STAFF_GROUP_LEADER_HIS
  - BFM_STAFF_GROUP_RELA
  - BFM_STAFF_HIS
  - BFM_STAFF_JOB
  - BFM_STAFF_ORG
  - BFM_STAFF_ORG_HIS
  - BFM_STAFF_RELA
  - BFM_STAFF_WORK_TIME
  - BFM_TABLE_EXT
  - BFM_TABLE_EXT_VALUE
  - BFM_TASK_STATE
  - BFM_USER
  - BFM_USER_DATA_PRIV
  - BFM_USER_GRID
  - BFM_USER_GROUP
  - BFM_USER_GROUP_PRIV
  - BFM_USER_HIS
  - BFM_USER_LAYOUT
  - BFM_USER_PARAMS
  - BFM_USER_PORTAL
  - BFM_USER_PRIV
  - BFM_USER_ROLE
  - BFM_USER_SP
  - BFM_WEAK_PASSWORD
  - BIZ_RULE
  - BUNDLE_COMPOSE_TYPE
  - BUNDLE_MEM_EVENT_RELA
  - BUNDLE_RESTRICT_RULE
  - BUNDLE_RESTRICT_TYPE
  - BUNDLE_SUBS_EVENT_RELA
  - BUNDLE_UNIT
  - BUNDLE_UNIT_INDEP_PROD_SPEC
  - BUNDLE_UNIT_SUBS_PLAN
  - BUNDLE_USER_MAPPING
  - BUSI_TYPE_DEF
  - BUSI_TYPE_VALUE_DEF
  - BUSINESS_MODULE
  - CATALOG
  - CHANNEL_TYPE
  - COC_DOMAIN
  - COMMON_LANG_OBJ_VALUE
  - CONTACT_CHANNEL
  - CPC_ACCT_ITEM_TYPE_PRIV
  - CPC_AIT_ADD_STAFF
  - CPC_AIT_DEDUCT_STAFF
  - CPC_RENT_GROUP
  - CPC_RENT_PRIORITY
  - DATA_TYPE
  - DEF_LANG
  - DEPEND_PROD_PACKAGE
  - DEPEND_PROD_SPEC
  - DOMAIN
  - DRAFT_ORDER_REASON
  - EXTERNAL_OFFER_MAP
  - GLOBAL_BO
  - GLOBAL_OBJECT
  - GLOBAL_RES
  - GOODS_PROD_PACKAGE
  - GOODS_PROD_SPEC
  - GROUP_TYPE
  - HOT_SALE_TYPE
  - INDEP_PROD_SPEC
  - INPUT_TYPE
  - LANGUAGE_TYPE
  - MEMO_EVENT_TYPE
  - MTN_BC_ROLE
  - MTN_ERICSSON_OFFER
  - MTN_ERICSSON_OFFER_PAYMENT
  - MULTY_LANG_ITEM
  - MULTY_LANG_ITEM_VALUE
  - NETWORK_TYPE
  - OBJ_ATTR
  - OFFER
  - OFFER_ACCT_ITEM_REL
  - OFFER_ACTION
  - OFFER_APPLY_AREA
  - OFFER_APPLY_CATG
  - OFFER_APPLY_CHANNEL
  - OFFER_APPLY_ORG
  - OFFER_APPLY_ROLE
  - OFFER_APPLY_STAFF
  - OFFER_ATTR
  - OFFER_ATTR_VALUE
  - OFFER_BUSI_TYPE
  - OFFER_CATG
  - OFFER_CATG_APPLY_CHANNEL
  - OFFER_CATG_MEM
  - OFFER_GROUP
  - OFFER_GROUP_MEM
  - OFFER_HOT_SALES
  - OFFER_PRICE_DISPLAY
  - OFFER_PRICE_VERSION
  - OFFER_PROMO_CATG
  - OFFER_PROMO_FULL_MINUS
  - OFFER_PROMO_GRP
  - OFFER_PROMO_GRP_MEM
  - OFFER_PROMO_MATCH_RULE
  - OFFER_PROMO_MEMBER
  - OFFER_PROMO_PRICE
  - OFFER_PROMOTION
  - OFFER_PROMOTION_TYPE
  - OFFER_RELA
  - OFFER_RULE
  - OFFER_TYPE
  - OFFER_VER
  - OPERATION_TYPE
  - ORDER_REASON
  - ORDER_REASON_ATTR
  - ORDER_REASON_ATTR_VALUE
  - ORDER_RELA_TYPE
  - ORDER_STATE
  - ORDER_TYPE
  - PARTY_TYPE
  - PRICE_PLAN
  - PRICE_PLAN_PACKAGE
  - PRICE_PLAN_TYPE
  - RELA_TYPE
  - RES_PROD_SPEC
  - RES_TYPE
  - SERV_SPEC_CATA_MEM
  - SERV_SPEC_CATALOG
  - SERV_SPEC_OPT
  - SERV_TYPE_CFS
  - SERV_TYPE_CFS_OPT_MAP
  - SERV_TYPE_EVENT_RELA
  - SERVICE_SPEC
  - SERVICE_SPEC_ATTR
  - SERVICE_SPEC_ATTR_VALUE
  - SERVICE_SPEC_RELA
  - SERVICE_TEMPLATE_ATTR
  - SERVICE_TEMPLATE_ATTR_VALUE
  - SUBS_EVENT
  - SUBS_EVENT_PROD_STATE_LIMIT
  - SUBS_EVENT_RELA
  - SUBS_EVENT_TYPE
  - SUBS_PLAN
  - SUBS_PLAN_OFFER_ATTR
  - SUBS_PLAN_OFFER_ATTR_VALUE
  - SUBS_PLAN_OFFER_SELECT
  - SUBS_PLAN_PRICE
  - SUBS_PRICE_PLAN
  - SUBS_RE_EVENT
  - SUBS_RELA_EXCLUSIVE
  - TAG
  - TAG_STYLE
  - TAG_TYPE
  - TEXT_ATTR
  - TFM_EVENT_SRC
  - TFM_EVENT_TYPE
  - TFM_SERV_LOG
  - TFM_SERV_LOG_RELA
  - TIME_SLICE
  - TIME_SLICE_GROUP
  - TIME_SLICE_GROUP_ITEM
  - TIME_UNIT
  - TIMES_LIMIT_RULE
  - TIMES_LIMIT_RULE_APPLY_OBJ
  - TVLOM_ACTIVITY
  - WF_DISPATCH_ORDER_STATE
  - WF_DISPATCH_ORDER_TYPE
  - WF_NODE_AUTO_FUCTION
  - WF_NODE_FUNCTION
  - WF_NODE_MANUAL_FUCTION
  - WF_WORK_ORDER_STATE
  defaultDataSourceName: ds0
  tables:
    AGREEMENT_ATTR_ORDER:
      actualDataNodes: ds${0..3}.AGREEMENT_ATTR_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: AGREEMENT_ATTR_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    AGREEMENT_MAINTAINER_ORDER:
      actualDataNodes: ds${0..3}.AGREEMENT_MAINTAINER_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: AGREEMENT_MAINTAINER_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    AGREEMENT_OBJ_ORDER:
      actualDataNodes: ds${0..3}.AGREEMENT_OBJ_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: AGREEMENT_OBJ_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    AGREEMENT_ORDER:
      actualDataNodes: ds${0..3}.AGREEMENT_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: AGREEMENT_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    BAL_SHARE_ITEM_ORDER:
      actualDataNodes: ds${0..3}.BAL_SHARE_ITEM_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BAL_SHARE_ITEM_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    BAL_SHARE_ORDER:
      actualDataNodes: ds${0..3}.BAL_SHARE_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BAL_SHARE_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    BC_MEMBER_ATTR_ORDER:
      actualDataNodes: ds${0..3}.BC_MEMBER_ATTR_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BC_MEMBER_ATTR_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    BC_MEMBER_ORDER:
      actualDataNodes: ds${0..3}.BC_MEMBER_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BC_MEMBER_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    BC_ROLE_ORDER:
      actualDataNodes: ds${0..3}.BC_ROLE_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BC_ROLE_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    BFM_PROCESS_BO:
      actualDataNodes: ds${0..3}.BFM_PROCESS_BO${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BFM_PROCESS_BO${PART_ID % 12}
          shardingColumn: PART_ID
    BILL_SPLIT_ORDER:
      actualDataNodes: ds${0..3}.BILL_SPLIT_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BILL_SPLIT_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    CART_ITEM:
      actualDataNodes: ds${0..3}.CART_ITEM${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CART_ITEM${PART_ID % 12}
          shardingColumn: PART_ID
    CART_ITEM_REL:
      actualDataNodes: ds${0..3}.CART_ITEM_REL${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CART_ITEM_REL${PART_ID % 12}
          shardingColumn: PART_ID
    CDR_PRINT_ORDER:
      actualDataNodes: ds${0..3}.CDR_PRINT_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CDR_PRINT_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    CREDIT_BALANCE_ORDER:
      actualDataNodes: ds${0..3}.CREDIT_BALANCE_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CREDIT_BALANCE_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    CREDIT_LIMIT_ORDER:
      actualDataNodes: ds${0..3}.CREDIT_LIMIT_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CREDIT_LIMIT_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    CUST_CONTACT:
      actualDataNodes: ds${0..3}.CUST_CONTACT${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_CONTACT${PART_ID % 12}
          shardingColumn: PART_ID
    CUST_CONTRACT_ITEM_SUBS_ORDER:
      actualDataNodes: ds${0..3}.CUST_CONTRACT_ITEM_SUBS_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_CONTRACT_ITEM_SUBS_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    CUST_ORDER:
      actualDataNodes: ds${0..3}.CUST_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    CUST_ORDER_ATTACH:
      actualDataNodes: ds${0..3}.CUST_ORDER_ATTACH${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_ORDER_ATTACH${PART_ID % 12}
          shardingColumn: PART_ID
    CUST_ORDER_ATTR:
      actualDataNodes: ds${0..3}.CUST_ORDER_ATTR${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_ORDER_ATTR${PART_ID % 12}
          shardingColumn: PART_ID
    CUST_ORDER_VER:
      actualDataNodes: ds${0..3}.CUST_ORDER_VER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_ORDER_VER${PART_ID % 12}
          shardingColumn: PART_ID
    CUST_ORDER_VER_ITEM:
      actualDataNodes: ds${0..3}.CUST_ORDER_VER_ITEM${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_ORDER_VER_ITEM${PART_ID % 12}
          shardingColumn: PART_ID
    CUST_PROF_ORDER:
      actualDataNodes: ds${0..3}.CUST_PROF_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_PROF_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    DELIVERY_ORDER:
      actualDataNodes: ds${0..3}.DELIVERY_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: DELIVERY_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    DELIVERY_ORDER_GOODS_ITEM:
      actualDataNodes: ds${0..3}.DELIVERY_ORDER_GOODS_ITEM${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: DELIVERY_ORDER_GOODS_ITEM${PART_ID % 12}
          shardingColumn: PART_ID
    DELIVERY_ORDER_ITEM:
      actualDataNodes: ds${0..3}.DELIVERY_ORDER_ITEM${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: DELIVERY_ORDER_ITEM${PART_ID % 12}
          shardingColumn: PART_ID
    DP_OFFER_ORDER:
      actualDataNodes: ds${0..3}.DP_OFFER_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: DP_OFFER_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    DP_OFFER_ORDER_ATTR:
      actualDataNodes: ds${0..3}.DP_OFFER_ORDER_ATTR${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: DP_OFFER_ORDER_ATTR${PART_ID % 12}
          shardingColumn: PART_ID
    FELLOW_NBR_ORDER:
      actualDataNodes: ds${0..3}.FELLOW_NBR_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: FELLOW_NBR_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    GOODS_ORDER:
      actualDataNodes: ds${0..3}.GOODS_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: GOODS_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    GOODS_ORDER_ATTR:
      actualDataNodes: ds${0..3}.GOODS_ORDER_ATTR${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: GOODS_ORDER_ATTR${PART_ID % 12}
          shardingColumn: PART_ID
    GOODS_SALE_ORDER:
      actualDataNodes: ds${0..3}.GOODS_SALE_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: GOODS_SALE_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    GOODS_SALE_ORDER_ATTR:
      actualDataNodes: ds${0..3}.GOODS_SALE_ORDER_ATTR${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: GOODS_SALE_ORDER_ATTR${PART_ID % 12}
          shardingColumn: PART_ID
    GOODS_SALE_ORDER_COUPON:
      actualDataNodes: ds${0..3}.GOODS_SALE_ORDER_COUPON${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: GOODS_SALE_ORDER_COUPON${PART_ID % 12}
          shardingColumn: PART_ID
    GOODS_SALE_ORDER_DETAIL:
      actualDataNodes: ds${0..3}.GOODS_SALE_ORDER_DETAIL${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: GOODS_SALE_ORDER_DETAIL${PART_ID % 12}
          shardingColumn: PART_ID
    GOODS_SALE_ORDER_REL:
      actualDataNodes: ds${0..3}.GOODS_SALE_ORDER_REL${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: GOODS_SALE_ORDER_REL${PART_ID % 12}
          shardingColumn: PART_ID
    GOODS_SALE_ORDER_RES:
      actualDataNodes: ds${0..3}.GOODS_SALE_ORDER_RES${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: GOODS_SALE_ORDER_RES${PART_ID % 12}
          shardingColumn: PART_ID
    HOME_CELL_ORDER:
      actualDataNodes: ds${0..3}.HOME_CELL_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: HOME_CELL_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    HOME_ZONE_ORDER:
      actualDataNodes: ds${0..3}.HOME_ZONE_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: HOME_ZONE_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    INDEP_PROD_ORDER_ATTR:
      actualDataNodes: ds${0..3}.INDEP_PROD_ORDER_ATTR${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: INDEP_PROD_ORDER_ATTR${PART_ID % 12}
          shardingColumn: PART_ID
    INDIVIDUAL_LIFECYCLE_ORDER:
      actualDataNodes: ds${0..3}.INDIVIDUAL_LIFECYCLE_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: INDIVIDUAL_LIFECYCLE_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    OFFER_PROD_RELA_ORDER:
      actualDataNodes: ds${0..3}.OFFER_PROD_RELA_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: OFFER_PROD_RELA_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_ITEM:
      actualDataNodes: ds${0..3}.ORDER_ITEM${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_ITEM${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_ITEM_ATTR:
      actualDataNodes: ds${0..3}.ORDER_ITEM_ATTR${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_ITEM_ATTR${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_ITEM_KPI:
      actualDataNodes: ds${0..3}.ORDER_ITEM_KPI${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_ITEM_KPI${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_ITEM_MEMO:
      actualDataNodes: ds${0..3}.ORDER_ITEM_MEMO${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_ITEM_MEMO${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_OTC_INST:
      actualDataNodes: ds${0..3}.ORDER_OTC_INST${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_OTC_INST${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_OTC_INST_PRICE:
      actualDataNodes: ds${0..3}.ORDER_OTC_INST_PRICE${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_OTC_INST_PRICE${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_OTC_INST_TAX:
      actualDataNodes: ds${0..3}.ORDER_OTC_INST_TAX${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_OTC_INST_TAX${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_OTC_INSTALLMENT_PLAN:
      actualDataNodes: ds${0..3}.ORDER_OTC_INSTALLMENT_PLAN${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_OTC_INSTALLMENT_PLAN${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_OTC_IP_DETAIL:
      actualDataNodes: ds${0..3}.ORDER_OTC_IP_DETAIL${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_OTC_IP_DETAIL${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_OTC_QRY_BENEFIT:
      actualDataNodes: ds${0..3}.ORDER_OTC_QRY_BENEFIT${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_OTC_QRY_BENEFIT${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_OTC_QRY_DEPOSIT:
      actualDataNodes: ds${0..3}.ORDER_OTC_QRY_DEPOSIT${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_OTC_QRY_DEPOSIT${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_OTC_QRY_ITEM:
      actualDataNodes: ds${0..3}.ORDER_OTC_QRY_ITEM${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_OTC_QRY_ITEM${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_OTC_QRY_PRICE:
      actualDataNodes: ds${0..3}.ORDER_OTC_QRY_PRICE${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_OTC_QRY_PRICE${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_OTC_QRY_PRICE_ADJUST:
      actualDataNodes: ds${0..3}.ORDER_OTC_QRY_PRICE_ADJUST${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_OTC_QRY_PRICE_ADJUST${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_OTC_QRY_TAX:
      actualDataNodes: ds${0..3}.ORDER_OTC_QRY_TAX${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_OTC_QRY_TAX${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_OTC_TRANSACTION:
      actualDataNodes: ds${0..3}.ORDER_OTC_TRANSACTION${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_OTC_TRANSACTION${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_OTC_TRANSACTION_DTL:
      actualDataNodes: ds${0..3}.ORDER_OTC_TRANSACTION_DTL${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_OTC_TRANSACTION_DTL${PART_ID % 12}
          shardingColumn: PART_ID
    ORDER_RELA:
      actualDataNodes: ds${0..3}.ORDER_RELA${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ORDER_RELA${PART_ID % 12}
          shardingColumn: PART_ID
    PARALLEL_UPGRADE_ORDER:
      actualDataNodes: ds${0..3}.PARALLEL_UPGRADE_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: PARALLEL_UPGRADE_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    POS_MACHINE_HIS:
      actualDataNodes: ds0.POS_MACHINE_HIS${0..11}
      tableStrategy:
        inline:
          algorithmExpression: POS_MACHINE_HIS${(PART_ID-1) % 12}
          shardingColumn: PART_ID
    POS_ORG_INFO_HIS:
      actualDataNodes: ds0.POS_ORG_INFO_HIS${0..11}
      tableStrategy:
        inline:
          algorithmExpression: POS_ORG_INFO_HIS${(PART_ID-1) % 12}
          shardingColumn: PART_ID
    PROMO_OFFER_ORDER:
      actualDataNodes: ds${0..3}.PROMO_OFFER_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: PROMO_OFFER_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    RES_ORDER:
      actualDataNodes: ds${0..3}.RES_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: RES_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    SHOPPING_CART:
      actualDataNodes: ds${0..3}.SHOPPING_CART${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SHOPPING_CART${PART_ID % 12}
          shardingColumn: PART_ID
    SIM_CARD_INFO_ORDER:
      actualDataNodes: ds${0..3}.SIM_CARD_INFO_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SIM_CARD_INFO_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    STAFF_DRAFT:
      actualDataNodes: ds${0..3}.STAFF_DRAFT${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: STAFF_DRAFT${PART_ID % 12}
          shardingColumn: PART_ID
    SUBS_BASE_ORDER:
      actualDataNodes: ds${0..3}.SUBS_BASE_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_BASE_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    SUBS_BIND_ORDER:
      actualDataNodes: ds${0..3}.SUBS_BIND_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_BIND_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    SUBS_CONTACT_MAN_ORDER:
      actualDataNodes: ds${0..3}.SUBS_CONTACT_MAN_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_CONTACT_MAN_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    SUBS_RELA_ATTR_ORDER:
      actualDataNodes: ds${0..3}.SUBS_RELA_ATTR_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_RELA_ATTR_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    SUBS_SOCIAL_ORDER:
      actualDataNodes: ds${0..3}.SUBS_SOCIAL_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_SOCIAL_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    TIMER_EVENT:
      actualDataNodes: ds${0..3}.TIMER_EVENT${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: TIMER_EVENT${PART_ID % 12}
          shardingColumn: PART_ID
    TIMER_EVENT_MEMO:
      actualDataNodes: ds${0..3}.TIMER_EVENT_MEMO${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: TIMER_EVENT_MEMO${PART_ID % 12}
          shardingColumn: PART_ID
    WF_DISPATCH_ORDER:
      actualDataNodes: ds${0..3}.WF_DISPATCH_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: WF_DISPATCH_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    WF_DISPATCH_ORDER_ACTION:
      actualDataNodes: ds${0..3}.WF_DISPATCH_ORDER_ACTION
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    WF_DISPATCH_ORDER_ACTION_ATTA:
      actualDataNodes: ds${0..3}.WF_DISPATCH_ORDER_ACTION_ATTA
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    WF_DISPATCH_ORDER_ACTION_ATTR:
      actualDataNodes: ds${0..3}.WF_DISPATCH_ORDER_ACTION_ATTR
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    WF_WARN_DETAIL:
      actualDataNodes: ds${0..3}.WF_WARN_DETAIL
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    WF_WARN_LOG:
      actualDataNodes: ds${0..3}.WF_WARN_LOG
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    WF_WORK_ORDER:
      actualDataNodes: ds${0..3}.WF_WORK_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: WF_WORK_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    WF_WORK_ORDER_ACTION:
      actualDataNodes: ds${0..3}.WF_WORK_ORDER_ACTION${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: WF_WORK_ORDER_ACTION${PART_ID % 12}
          shardingColumn: PART_ID
    WF_WORK_ORDER_ACTION_ATTA:
      actualDataNodes: ds${0..3}.WF_WORK_ORDER_ACTION_ATTA
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    WF_WORK_ORDER_ACTION_ATTR:
      actualDataNodes: ds${0..3}.WF_WORK_ORDER_ACTION_ATTR
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    WF_WORK_ORDER_ATTR:
      actualDataNodes: ds${0..3}.WF_WORK_ORDER_ATTR${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: WF_WORK_ORDER_ATTR${PART_ID % 12}
          shardingColumn: PART_ID
    WF_WORK_ORDER_HIS:
      actualDataNodes: ds${0..3}.WF_WORK_ORDER_HIS${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: WF_WORK_ORDER_HIS${PART_ID % 12}
          shardingColumn: PART_ID