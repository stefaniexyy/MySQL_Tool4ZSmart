schemaName: cc
dataSources:
  ds0:
    url: jdbc:mysql://10.159.0.101:3307/cc_0001?serverTimezone=UTC&useSSL=false
    username: cc
    password: 1jian8Shu)
    connectionTimeoutMilliseconds: 30000
    idleTimeoutMilliseconds: 60000
    maxLifetimeMilliseconds: 1800000
    maxPoolSize: 50
  ds1:
    url: jdbc:mysql://10.159.0.103:3307/cc_0002?serverTimezone=UTC&useSSL=false
    username: cc
    password: 1jian8Shu)
    connectionTimeoutMilliseconds: 30000
    idleTimeoutMilliseconds: 60000
    maxLifetimeMilliseconds: 1800000
    maxPoolSize: 50
  ds2:
    url: jdbc:mysql://10.159.0.105:3307/cc_0003?serverTimezone=UTC&useSSL=false
    username: cc
    password: 1jian8Shu)
    connectionTimeoutMilliseconds: 30000
    idleTimeoutMilliseconds: 60000
    maxLifetimeMilliseconds: 1800000
    maxPoolSize: 50
  ds3:
    url: jdbc:mysql://10.159.0.107:3307/cc_0004?serverTimezone=UTC&useSSL=false
    username: cc
    password: 1jian8Shu)
    connectionTimeoutMilliseconds: 30000
    idleTimeoutMilliseconds: 60000
    maxLifetimeMilliseconds: 1800000
    maxPoolSize: 50

shardingRule:
  bindingTables:
  - BAL_SHARE_ITEM_ORDER,BFM_PROCESS_BO,WF_DISPATCH_ORDER,WF_WORK_ORDER,WF_WORK_ORDER_ACTION,WF_WORK_ORDER_ATTR,WF_WORK_ORDER_HIS
  - ACCOUNT_MANAGER_CUST,ACCOUNT_MANAGER_CUST_HIS,ACCT,ACCT_ATTR_VALUE,ACCT_ATTR_VALUE_HIS,ACCT_DELIVER_METHOD,ACCT_HIS,ACCT_UPP_INST,ACCT_UPP_INST_HIS,ACCT_UPP_INST_VALUE,ACCT_UPP_INST_VALUE_HIS,AGREEMENT_INST,AGREEMENT_INST_HIS,AGREEMENT_MAINTAINER_INST,AGREEMENT_MAINTAINER_INST_HIS,AGREEMENT_OBJ_INST,BANK_ACCT,BC_BUSINESS_HOUR,BC_MEMBER,BC_MEMBER_HIS,BC_ROLE_INST,BC_ROLE_INST_HIS,BIZ_CUST,BIZ_CUST_HIS,CC_ACCT_HIS,CONTACT_MAN,CONTACT_MAN_ATTR_VALUE,CONTACT_MAN_HIS,CONTRACT_ATTR_VAL,CONTRACT_ITEM_OFFER_ATTR,CONTRACT_ITEM_OFFER_ATTR_VAL,CREDIT_CARD,CUST,CUST_ATTR_VALUE,CUST_ATTR_VALUE_HIS,CUST_CONTACT,CUST_CONTRACT,CUST_CONTRACT_ITEM,CUST_CONTRACT_ITEM_ACCT,CUST_CONTRACT_ITEM_ACCT_HIS,CUST_CONTRACT_ITEM_PO_ATTR,CUST_CONTRACT_ITEM_PO_RELA,CUST_CONTRACT_ITEM_SUBS,CUST_CONTRACT_ITEM_SUBS_HIS,CUST_CONTRACT_ITEM_SUBS_RELA,CUST_DELIVERY_ADDRESS,CUST_DOC,CUST_HABIT,CUST_HIS,CUST_PRICE_BOOK_HANDSET,CUST_PRICE_BOOK_HANDSET_RATE,CUST_PRICE_BOOK_HANDSET_REBATE,CUST_PRICE_BOOK_OTC_BENEFIT,CUST_PRICE_BOOK_OTC_RATE,CUST_PRICE_BOOK_PRICE_PLAN,CUST_PRICE_BOOK_S_PARAM_VALUE,CUST_PRICE_BOOK_VER,CUST_PRICE_P_T_PARAM_CATEGORY,CUST_PRICE_PLAN_T_PARAM,CUST_PRICE_PLAN_T_PARAM_VALUE,CUST_SOCIAL_NBR,CUST_SPECIAL_GROUP,CUST_SPECIAL_GROUP_HIS,DUNNING_AS_GROUP,DUNNING_AS_GROUP_MEM,DUNNING_AS_GROUP_MEM_SUBS,FELLOW_NBR,FELLOW_NBR_HIS,GROUP_MARK_DETAIL,MANDATE,MANDATE_HIS,MEMBER_WORK_HOUR,MTN_BONUS_CHANGE,MTN_CUST_NOTIFICATION,MTN_MENU_SEG,MTN_PHONE_DEALS_MEMBER,MTN_TRANSFER,MTN_TRANSFER_ACM,OFFER_PROD_INST_RELA,OFFER_PROD_INST_RELA_HIS,PAYMENT_PROFILE,PROD,PROD_ATTR_VALUE,PROD_ATTR_VALUE_HIS,PROD_HIS,PROD_INDIVIDUAL_LIFECYCLE,PROD_STATE_CHANGE_LOG,PROD_STATE_DETAIL,SPC_BAL_SHARE,SUBS,SUBS_GOODS_INST,SUBS_GOODS_INST_ATTR,SUBS_GOODS_INST_ATTR_HIS,SUBS_GOODS_INST_HIS,SUBS_HIS,SUBS_HOME_CELL,SUBS_HOME_ZONE,SUBS_SPECIAL_GROUP,SUBS_SPECIAL_GROUP_HIS,SUBS_UPP_INST,SUBS_UPP_INST_HIS,SUBS_UPP_INST_VALUE,SUBS_UPP_INST_VALUE_HIS,USED_RES
  - CONTRACT_ITEM_ATTR_VAL,CONTRACT_ITEM_OFFER_ATTR
  - SUBS_IDENTIFY,SUBS_IDENTIFY_HIS
  broadcastTables:
  - ACCT_ATTR
  - AGREEMENT
  - AGREEMENT_ACTION_LIMIT
  - AGREEMENT_APPLY
  - AGREEMENT_GROUP
  - AGREEMENT_GROUP_MEMBER
  - AGREEMENT_INSTALLMENT
  - AGREEMENT_LEVEL_ACTION
  - AGREEMENT_MAINTAINER
  - AGREEMENT_MAINTAINER_OBJ
  - AGREEMENT_NOTIFY
  - AGREEMENT_OBJ
  - AGREEMENT_OBJ_TYPE
  - AGREEMENT_OBJ_TYPE_ACTION
  - AGREEMENT_PENALTY
  - AGREEMENT_PENALTY_TPL
  - AGREEMENT_PRICE
  - AGREEMENT_QUANTITY_LIMIT
  - AGREEMENT_RELA
  - AGREEMENT_RELA_TYPE
  - AGREEMENT_RSTR
  - AGREEMENT_RULE
  - AGREEMENT_TPL
  - AGREEMENT_TYPE
  - AGREEMENT_TYPE_TAG
  - AGREEMENT_VER
  - ALL_SERV_TYPE
  - ATTR
  - ATTR_APPLY_CATG
  - ATTR_APPLY_CHANNEL
  - ATTR_CATG
  - ATTR_DRIVER
  - ATTR_GROUP
  - ATTR_GROUP_MEM
  - ATTR_POPEDIT
  - ATTR_TYPE
  - ATTR_VALUE
  - ATTR_VALUE_LINKAGE
  - BANK
  - BANK_CARD_TYPE
  - BANK_CARD_TYPE_INST
  - BASE_ATTR
  - BATCH_UPDATE_DATA
  - BATCH_UPDATE_TABLE
  - BC_MEMBER_TYPE
  - BC_ROLE
  - BFM_AD_CONFIG
  - BFM_APP
  - BFM_AREA
  - BFM_BULLETIN
  - BFM_BULLETIN_LEVEL
  - BFM_BULLETIN_RECIPIENT
  - BFM_BULLETIN_TMPL
  - BFM_BULLETIN_TYPE
  - BFM_BULLETIN_VIEW
  - BFM_COMPONENT_PRIV
  - BFM_DATA_PRIV
  - BFM_DIR
  - BFM_EVENT_CODE
  - BFM_EVENT_SRC
  - BFM_EVENT_TYPE
  - BFM_GROUP_DATA_PRIV
  - BFM_JOB
  - BFM_JOB_LAYOUT
  - BFM_JOB_ROLE
  - BFM_JOB_USER_GROUP
  - BFM_JOB_WORK_TIME
  - BFM_LAYOUT_DEF
  - BFM_LAYOUT_DEF_HIS
  - BFM_MENU
  - BFM_MENU_DIR
  - BFM_MENU_FAVORITE
  - BFM_MODULE
  - BFM_MVNO_HIS
  - BFM_ORG
  - BFM_ORG_JOB
  - BFM_ORG_TYPE
  - BFM_ORG_WORK_TIME
  - BFM_PARAMS
  - BFM_PART_SP
  - BFM_PORTAL
  - BFM_PORTAL_BIZ_PARAM
  - BFM_PORTAL_CHANNEL
  - BFM_PORTAL_INDEX
  - BFM_PORTAL_MENU
  - BFM_PORTAL_SP
  - BFM_PORTLET_SCOPE
  - BFM_PORTLET_TYPE
  - BFM_PORTLETS
  - BFM_PRIV
  - BFM_PRIV_TYPE
  - BFM_PROCESS_ACTIVITY
  - BFM_PROCESS_STATE
  - BFM_ROLE
  - BFM_ROLE_DATA_PRIV
  - BFM_ROLE_PORTAL
  - BFM_ROLE_PRIV
  - BFM_SECURITY_RULE
  - BFM_SERV_LOG
  - BFM_STAFF
  - BFM_STAFF_GROUP_LEADER_HIS
  - BFM_STAFF_GROUP_RELA
  - BFM_STAFF_HIS
  - BFM_STAFF_JOB
  - BFM_STAFF_ORG
  - BFM_STAFF_ORG_HIS
  - BFM_STAFF_RELA
  - BFM_STAFF_WORK_TIME
  - BFM_TABLE_EXT
  - BFM_TABLE_EXT_VALUE
  - BFM_TASK_STATE
  - BFM_USER
  - BFM_USER_DATA_PRIV
  - BFM_USER_GRID
  - BFM_USER_GROUP
  - BFM_USER_GROUP_PRIV
  - BFM_USER_HIS
  - BFM_USER_LAYOUT
  - BFM_USER_PARAMS
  - BFM_USER_PORTAL
  - BFM_USER_PRIV
  - BFM_USER_ROLE
  - BFM_USER_SP
  - BFM_WEAK_PASSWORD
  - BILL_SPLIT_TEMPLATE
  - BILL_SPLIT_TMPL_GRP
  - BILL_SPLIT_TMPL_GRP_MEM
  - BILLING_CYCLE
  - BILLING_CYCLE_TYPE
  - BIND_TYPE
  - BIND_TYPE_ATTR
  - BIZ_RULE
  - BLACKLIST_TYPE
  - BS_TEMPLATE
  - BS_TEMPLATE_GROUP
  - BS_TEMPLATE_GROUP_MEM
  - BS_TEMPLATE_ITEM
  - BUNDLE_COMPOSE_TYPE
  - BUNDLE_MEM_EVENT_RELA
  - BUNDLE_RESTRICT_RULE
  - BUNDLE_RESTRICT_TYPE
  - BUNDLE_SUBS_EVENT_RELA
  - BUNDLE_UNIT
  - BUNDLE_UNIT_INDEP_PROD_SPEC
  - BUNDLE_UNIT_SUBS_PLAN
  - BUNDLE_USER_MAPPING
  - BUSI_TYPE_DEF
  - BUSI_TYPE_VALUE_DEF
  - BUSINESS_HOUR
  - BUSINESS_HOUR_TEMPLATE
  - BUSINESS_HOUR_TEMPLATE_DETAIL
  - BUSINESS_MODULE
  - CATALOG
  - CATG
  - CC_TYPE
  - CERT_TYPE
  - CHANNEL_TYPE
  - COMMON_LANG_OBJ_VALUE
  - CONTACT_CHANNEL
  - CONTACT_MAN_ATTR
  - CONTACT_MAN_TYPE
  - CPC_ACCT_ITEM_TYPE_PRIV
  - CPC_AIT_ADD_STAFF
  - CPC_AIT_DEDUCT_STAFF
  - CPC_RENT_GROUP
  - CPC_RENT_PRIORITY
  - CUST_CREDIT_GRADE
  - CUST_FLAG
  - CUST_RT_TABLE_TYPE
  - CUST_TYPE
  - CUST_TYPE_ATTR
  - DATA_TYPE
  - DEF_LANG
  - DELIVER_METHOD
  - DEPEND_PROD_PACKAGE
  - DEPEND_PROD_SPEC
  - DIY_PARAM_ENUM_VALUE
  - DOC_TYPE
  - DOMAIN
  - EXTERNAL_OFFER_MAP
  - FELLOW_NBR_TYPE
  - GEO_HOME_ZONE
  - GEO_HOME_ZONE_CATG
  - GEO_HOME_ZONE_CATG_RLAT
  - GEO_NET_ZONE
  - GLOBAL_BO
  - GLOBAL_OBJECT
  - GLOBAL_RES
  - GOODS_PROD_PACKAGE
  - GOODS_PROD_SPEC
  - GROUP_TYPE
  - HABIT_TYPE
  - HOME_ZONE
  - HOT_SALE_TYPE
  - IBAN_FORMAT
  - IMP_GRADE
  - INDEP_PROD_SPEC
  - INDIVIDUAL_DATA_RECORD_V9CUSTC
  - INDUSTRY
  - INPUT_TYPE
  - LANGUAGE_TYPE
  - LIFECYCLE_APPLY
  - MTN_BC_ROLE
  - MTN_CUST_SUB_TYPE
  - MTN_ERICSSON_OFFER
  - MTN_ERICSSON_OFFER_PAYMENT
  - MULTY_LANG_ITEM
  - MULTY_LANG_ITEM_VALUE
  - NETWORK_TYPE
  - OBJ_ATTR
  - OCCUPATION
  - OFFER
  - OFFER_ACCT_ITEM_REL
  - OFFER_ACTION
  - OFFER_APPLY_AREA
  - OFFER_APPLY_CATG
  - OFFER_APPLY_CHANNEL
  - OFFER_APPLY_ORG
  - OFFER_APPLY_ROLE
  - OFFER_APPLY_STAFF
  - OFFER_ATTR
  - OFFER_ATTR_VALUE
  - OFFER_BUSI_TYPE
  - OFFER_CATG
  - OFFER_CATG_APPLY_CHANNEL
  - OFFER_CATG_MEM
  - OFFER_GROUP
  - OFFER_GROUP_MEM
  - OFFER_HOT_SALES
  - OFFER_PRICE_DISPLAY
  - OFFER_PRICE_VERSION
  - OFFER_PROMO_CATG
  - OFFER_PROMO_FULL_MINUS
  - OFFER_PROMO_GRP
  - OFFER_PROMO_GRP_MEM
  - OFFER_PROMO_MATCH_RULE
  - OFFER_PROMO_MEMBER
  - OFFER_PROMO_PRICE
  - OFFER_PROMOTION
  - OFFER_PROMOTION_TYPE
  - OFFER_RELA
  - OFFER_RULE
  - OFFER_TYPE
  - OFFER_VER
  - PARTY_TYPE
  - PAYMENT_METHOD
  - PAYMENT_TYPE
  - PRICE_PLAN
  - PRICE_PLAN_PACKAGE
  - PRICE_PLAN_TYPE
  - PROD_EVENT_DIAGRAM
  - PROD_SPEC_BS_TEMPLATE
  - PROD_STATE
  - PROD_STD_STATE
  - RELA_TYPE
  - RELIGION
  - RES_PROD_SPEC
  - ROLE_TYPE
  - SEPA_BANK
  - SERV_SPEC_CATA_MEM
  - SERV_SPEC_CATALOG
  - SERV_SPEC_OPT
  - SERV_TYPE_CFS
  - SERV_TYPE_CFS_OPT_MAP
  - SERV_TYPE_EVENT_RELA
  - SERVICE_BC
  - SERVICE_SPEC
  - SERVICE_SPEC_ATTR
  - SERVICE_SPEC_ATTR_VALUE
  - SERVICE_SPEC_RELA
  - SERVICE_TEMPLATE_ATTR
  - SERVICE_TEMPLATE_ATTR_VALUE
  - SPECIAL_GROUP_TYPE
  - STAND_ADDRESS
  - SUBS_EVENT
  - SUBS_EVENT_PROD_STATE_LIMIT
  - SUBS_EVENT_RELA
  - SUBS_EVENT_TYPE
  - SUBS_PLAN
  - SUBS_PLAN_OFFER_ATTR
  - SUBS_PLAN_OFFER_ATTR_VALUE
  - SUBS_PLAN_OFFER_SELECT
  - SUBS_PLAN_PRICE
  - SUBS_PRICE_PLAN
  - SUBS_RE_EVENT
  - SUBS_RELA_EXCLUSIVE
  - TAG
  - TAG_STYLE
  - TAG_TYPE
  - TEXT_ATTR
  - TFM_EVENT_SRC
  - TFM_EVENT_TYPE
  - TFM_SERV_LOG
  - TFM_SERV_LOG_RELA
  - TIME_SLICE
  - TIME_SLICE_GROUP
  - TIME_SLICE_GROUP_ITEM
  - TIME_UNIT
  - TIMES_LIMIT_RULE
  - TIMES_LIMIT_RULE_APPLY_OBJ
  - TITLE
  - TVLOM_ACTIVITY
  - WF_DISPATCH_ORDER_STATE
  - WF_DISPATCH_ORDER_TYPE
  - WF_NODE_AUTO_FUCTION
  - WF_NODE_FUNCTION
  - WF_NODE_MANUAL_FUCTION
  - WF_WORK_ORDER_STATE
  defaultDataSourceName: ds0
  tables:
    ACCOUNT_MANAGER_CUST:
      actualDataNodes: ds${0..3}.ACCOUNT_MANAGER_CUST${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ACCOUNT_MANAGER_CUST${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    ACCOUNT_MANAGER_CUST_HIS:
      actualDataNodes: ds${0..3}.ACCOUNT_MANAGER_CUST_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ACCOUNT_MANAGER_CUST_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    ACCT:
      actualDataNodes: ds${0..3}.ACCT${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ACCT${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    ACCT_ATTR_VALUE:
      actualDataNodes: ds${0..3}.ACCT_ATTR_VALUE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ACCT_ATTR_VALUE${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    ACCT_ATTR_VALUE_HIS:
      actualDataNodes: ds${0..3}.ACCT_ATTR_VALUE_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ACCT_ATTR_VALUE_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    ACCT_DELIVER_METHOD:
      actualDataNodes: ds${0..3}.ACCT_DELIVER_METHOD${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ACCT_DELIVER_METHOD${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    ACCT_HIS:
      actualDataNodes: ds${0..3}.ACCT_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ACCT_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    ACCT_IDENTIFY:
      actualDataNodes: ds${0..3}.ACCT_IDENTIFY
      databaseStrategy:
        inline:
          algorithmExpression: ds${Math.abs(IDENTIFY_VALUE.hashCode() % 4)}
          shardingColumn: IDENTIFY_VALUE
    ACCT_UPP_INST:
      actualDataNodes: ds${0..3}.ACCT_UPP_INST${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ACCT_UPP_INST${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    ACCT_UPP_INST_HIS:
      actualDataNodes: ds${0..3}.ACCT_UPP_INST_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ACCT_UPP_INST_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    ACCT_UPP_INST_VALUE:
      actualDataNodes: ds${0..3}.ACCT_UPP_INST_VALUE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ACCT_UPP_INST_VALUE${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    ACCT_UPP_INST_VALUE_HIS:
      actualDataNodes: ds${0..3}.ACCT_UPP_INST_VALUE_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: ACCT_UPP_INST_VALUE_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    AGREEMENT_INST:
      actualDataNodes: ds${0..3}.AGREEMENT_INST${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: AGREEMENT_INST${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    AGREEMENT_INST_HIS:
      actualDataNodes: ds${0..3}.AGREEMENT_INST_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: AGREEMENT_INST_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    AGREEMENT_MAINTAINER_INST:
      actualDataNodes: ds${0..3}.AGREEMENT_MAINTAINER_INST${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: AGREEMENT_MAINTAINER_INST${Long.valueOf(CUST_ID) %
            32}
          shardingColumn: CUST_ID
    AGREEMENT_MAINTAINER_INST_HIS:
      actualDataNodes: ds${0..3}.AGREEMENT_MAINTAINER_INST_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: AGREEMENT_MAINTAINER_INST_HIS${Long.valueOf(CUST_ID)
            % 32}
          shardingColumn: CUST_ID
    AGREEMENT_OBJ_INST:
      actualDataNodes: ds${0..3}.AGREEMENT_OBJ_INST${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: AGREEMENT_OBJ_INST${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    BANK_ACCT:
      actualDataNodes: ds${0..3}.BANK_ACCT${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BANK_ACCT${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    BC_BUSINESS_HOUR:
      actualDataNodes: ds${0..3}.BC_BUSINESS_HOUR${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BC_BUSINESS_HOUR${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    BC_MEMBER:
      actualDataNodes: ds${0..3}.BC_MEMBER${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BC_MEMBER${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    BC_MEMBER_HIS:
      actualDataNodes: ds${0..3}.BC_MEMBER_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BC_MEMBER_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    BC_ROLE_INST:
      actualDataNodes: ds${0..3}.BC_ROLE_INST${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BC_ROLE_INST${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    BC_ROLE_INST_HIS:
      actualDataNodes: ds${0..3}.BC_ROLE_INST_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BC_ROLE_INST_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    BFM_PROCESS_BO:
      actualDataNodes: ds${0..3}.BFM_PROCESS_BO${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BFM_PROCESS_BO${PART_ID % 12}
          shardingColumn: PART_ID
    BILL_SPC_ERROR_MESSAGE_HIS:
      actualDataNodes: ds0.BILL_SPC_ERROR_MESSAGE_HIS${0..30}
      tableStrategy:
        inline:
          algorithmExpression: BILL_SPC_ERROR_MESSAGE_HIS${(PART_ID-1) % 31}
          shardingColumn: PART_ID
    BIZ_CUST:
      actualDataNodes: ds${0..3}.BIZ_CUST${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BIZ_CUST${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    BIZ_CUST_HIS:
      actualDataNodes: ds${0..3}.BIZ_CUST_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: BIZ_CUST_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CC_ACCT_HIS:
      actualDataNodes: ds${0..3}.CC_ACCT_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CC_ACCT_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CERT:
      actualDataNodes: ds${0..3}.CERT
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    CERT_HIS:
      actualDataNodes: ds${0..3}.CERT_HIS
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    CONTACT_MAN:
      actualDataNodes: ds${0..3}.CONTACT_MAN${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CONTACT_MAN${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CONTACT_MAN_ATTR_VALUE:
      actualDataNodes: ds${0..3}.CONTACT_MAN_ATTR_VALUE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CONTACT_MAN_ATTR_VALUE${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CONTACT_MAN_HIS:
      actualDataNodes: ds${0..3}.CONTACT_MAN_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CONTACT_MAN_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CONTRACT_ATTR_VAL:
      actualDataNodes: ds${0..3}.CONTRACT_ATTR_VAL${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CONTRACT_ATTR_VAL${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CONTRACT_ITEM_ATTR_VAL:
      actualDataNodes: ds${0..3}.CONTRACT_ITEM_ATTR_VAL${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CONTRACT_ITEM_ATTR_VAL${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CONTRACT_ITEM_OFFER_ATTR:
      actualDataNodes: ds${0..3}.CONTRACT_ITEM_OFFER_ATTR${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CONTRACT_ITEM_OFFER_ATTR${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CONTRACT_ITEM_OFFER_ATTR_VAL:
      actualDataNodes: ds${0..3}.CONTRACT_ITEM_OFFER_ATTR_VAL${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CONTRACT_ITEM_OFFER_ATTR_VAL${Long.valueOf(CUST_ID)
            % 32}
          shardingColumn: CUST_ID
    CREDIT_CARD:
      actualDataNodes: ds${0..3}.CREDIT_CARD${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CREDIT_CARD${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST:
      actualDataNodes: ds${0..3}.CUST${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_ATTR_VALUE:
      actualDataNodes: ds${0..3}.CUST_ATTR_VALUE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_ATTR_VALUE${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_ATTR_VALUE_HIS:
      actualDataNodes: ds${0..3}.CUST_ATTR_VALUE_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_ATTR_VALUE_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_BILL_DELIVERY_INFO:
      actualDataNodes: ds${0..3}.CUST_BILL_DELIVERY_INFO
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    CUST_BILL_DELIVERY_INFO_HIS:
      actualDataNodes: ds${0..3}.CUST_BILL_DELIVERY_INFO_HIS
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    CUST_CONTACT:
      actualDataNodes: ds${0..3}.CUST_CONTACT${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_CONTACT${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_CONTRACT:
      actualDataNodes: ds${0..3}.CUST_CONTRACT${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_CONTRACT${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_CONTRACT_ITEM:
      actualDataNodes: ds${0..3}.CUST_CONTRACT_ITEM${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_CONTRACT_ITEM${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_CONTRACT_ITEM_ACCT:
      actualDataNodes: ds${0..3}.CUST_CONTRACT_ITEM_ACCT${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_CONTRACT_ITEM_ACCT${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_CONTRACT_ITEM_ACCT_HIS:
      actualDataNodes: ds${0..3}.CUST_CONTRACT_ITEM_ACCT_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_CONTRACT_ITEM_ACCT_HIS${Long.valueOf(CUST_ID)
            % 32}
          shardingColumn: CUST_ID
    CUST_CONTRACT_ITEM_PO_ATTR:
      actualDataNodes: ds${0..3}.CUST_CONTRACT_ITEM_PO_ATTR${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_CONTRACT_ITEM_PO_ATTR${Long.valueOf(CUST_ID) %
            32}
          shardingColumn: CUST_ID
    CUST_CONTRACT_ITEM_PO_RELA:
      actualDataNodes: ds${0..3}.CUST_CONTRACT_ITEM_PO_RELA${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_CONTRACT_ITEM_PO_RELA${Long.valueOf(CUST_ID) %
            32}
          shardingColumn: CUST_ID
    CUST_CONTRACT_ITEM_SUBS:
      actualDataNodes: ds${0..3}.CUST_CONTRACT_ITEM_SUBS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_CONTRACT_ITEM_SUBS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_CONTRACT_ITEM_SUBS_HIS:
      actualDataNodes: ds${0..3}.CUST_CONTRACT_ITEM_SUBS_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_CONTRACT_ITEM_SUBS_HIS${Long.valueOf(CUST_ID)
            % 32}
          shardingColumn: CUST_ID
    CUST_CONTRACT_ITEM_SUBS_RELA:
      actualDataNodes: ds${0..3}.CUST_CONTRACT_ITEM_SUBS_RELA${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_CONTRACT_ITEM_SUBS_RELA${Long.valueOf(CUST_ID)
            % 32}
          shardingColumn: CUST_ID
    CUST_DELIVERY_ADDRESS:
      actualDataNodes: ds${0..3}.CUST_DELIVERY_ADDRESS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_DELIVERY_ADDRESS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_DOC:
      actualDataNodes: ds${0..3}.CUST_DOC${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_DOC${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_HABIT:
      actualDataNodes: ds${0..3}.CUST_HABIT${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_HABIT${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_HIS:
      actualDataNodes: ds${0..3}.CUST_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_IDENTIFY:
      actualDataNodes: ds${0..3}.CUST_IDENTIFY
      databaseStrategy:
        inline:
          algorithmExpression: ds${Math.abs(IDENTIFY_VALUE.hashCode() % 4)}
          shardingColumn: IDENTIFY_VALUE
    CUST_IMAGE:
      actualDataNodes: ds${0..3}.CUST_IMAGE
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    CUST_PRICE_BOOK_HANDSET:
      actualDataNodes: ds${0..3}.CUST_PRICE_BOOK_HANDSET${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_PRICE_BOOK_HANDSET${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_PRICE_BOOK_HANDSET_RATE:
      actualDataNodes: ds${0..3}.CUST_PRICE_BOOK_HANDSET_RATE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_PRICE_BOOK_HANDSET_RATE${Long.valueOf(CUST_ID)
            % 32}
          shardingColumn: CUST_ID
    CUST_PRICE_BOOK_HANDSET_REBATE:
      actualDataNodes: ds${0..3}.CUST_PRICE_BOOK_HANDSET_REBATE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_PRICE_BOOK_HANDSET_REBATE${Long.valueOf(CUST_ID)
            % 32}
          shardingColumn: CUST_ID
    CUST_PRICE_BOOK_OTC_BENEFIT:
      actualDataNodes: ds${0..3}.CUST_PRICE_BOOK_OTC_BENEFIT${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_PRICE_BOOK_OTC_BENEFIT${Long.valueOf(CUST_ID)
            % 32}
          shardingColumn: CUST_ID
    CUST_PRICE_BOOK_OTC_RATE:
      actualDataNodes: ds${0..3}.CUST_PRICE_BOOK_OTC_RATE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_PRICE_BOOK_OTC_RATE${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_PRICE_BOOK_PRICE_PLAN:
      actualDataNodes: ds${0..3}.CUST_PRICE_BOOK_PRICE_PLAN${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_PRICE_BOOK_PRICE_PLAN${Long.valueOf(CUST_ID) %
            32}
          shardingColumn: CUST_ID
    CUST_PRICE_BOOK_S_PARAM_VALUE:
      actualDataNodes: ds${0..3}.CUST_PRICE_BOOK_S_PARAM_VALUE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_PRICE_BOOK_S_PARAM_VALUE${Long.valueOf(CUST_ID)
            % 32}
          shardingColumn: CUST_ID
    CUST_PRICE_BOOK_VER:
      actualDataNodes: ds${0..3}.CUST_PRICE_BOOK_VER${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_PRICE_BOOK_VER${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_PRICE_P_T_PARAM_CATEGORY:
      actualDataNodes: ds${0..3}.CUST_PRICE_P_T_PARAM_CATEGORY${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_PRICE_P_T_PARAM_CATEGORY${Long.valueOf(CUST_ID)
            % 32}
          shardingColumn: CUST_ID
    CUST_PRICE_PLAN_T_PARAM:
      actualDataNodes: ds${0..3}.CUST_PRICE_PLAN_T_PARAM${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_PRICE_PLAN_T_PARAM${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_PRICE_PLAN_T_PARAM_VALUE:
      actualDataNodes: ds${0..3}.CUST_PRICE_PLAN_T_PARAM_VALUE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_PRICE_PLAN_T_PARAM_VALUE${Long.valueOf(CUST_ID)
            % 32}
          shardingColumn: CUST_ID
    CUST_SOCIAL_NBR:
      actualDataNodes: ds${0..3}.CUST_SOCIAL_NBR${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_SOCIAL_NBR${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_SPECIAL_GROUP:
      actualDataNodes: ds${0..3}.CUST_SPECIAL_GROUP${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_SPECIAL_GROUP${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    CUST_SPECIAL_GROUP_HIS:
      actualDataNodes: ds${0..3}.CUST_SPECIAL_GROUP_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: CUST_SPECIAL_GROUP_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    DUNNING_AS_GROUP:
      actualDataNodes: ds${0..3}.DUNNING_AS_GROUP${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: DUNNING_AS_GROUP${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    DUNNING_AS_GROUP_HIS:
      actualDataNodes: ds${0..3}.DUNNING_AS_GROUP_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: DUNNING_AS_GROUP_HIS${Long.valueOf(PART_ID) % 32}
          shardingColumn: PART_ID
    DUNNING_AS_GROUP_MEM:
      actualDataNodes: ds${0..3}.DUNNING_AS_GROUP_MEM${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: DUNNING_AS_GROUP_MEM${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    DUNNING_AS_GROUP_MEM_HIS:
      actualDataNodes: ds${0..3}.DUNNING_AS_GROUP_MEM_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: DUNNING_AS_GROUP_MEM_HIS${Long.valueOf(PART_ID) % 32}
          shardingColumn: PART_ID
    DUNNING_AS_GROUP_MEM_SUBS:
      actualDataNodes: ds${0..3}.DUNNING_AS_GROUP_MEM_SUBS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: DUNNING_AS_GROUP_MEM_SUBS${Long.valueOf(CUST_ID) %
            32}
          shardingColumn: CUST_ID
    DUNNING_AS_GROUP_MEM_SUBS_HIS:
      actualDataNodes: ds${0..3}.DUNNING_AS_GROUP_MEM_SUBS_HIS${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: DUNNING_AS_GROUP_MEM_SUBS_HIS${Long.valueOf(PART_ID)
            % 12}
          shardingColumn: PART_ID
    FELLOW_NBR:
      actualDataNodes: ds${0..3}.FELLOW_NBR${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: FELLOW_NBR${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    FELLOW_NBR_HIS:
      actualDataNodes: ds${0..3}.FELLOW_NBR_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: FELLOW_NBR_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    GROUP_MARK_DETAIL:
      actualDataNodes: ds${0..3}.GROUP_MARK_DETAIL${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: GROUP_MARK_DETAIL${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    MANAGER_CONTACT_MAN:
      actualDataNodes: ds${0..3}.MANAGER_CONTACT_MAN
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    MANAGER_CONTACT_MAN_HIS:
      actualDataNodes: ds${0..3}.MANAGER_CONTACT_MAN_HIS
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    MANDATE:
      actualDataNodes: ds${0..3}.MANDATE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: MANDATE${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    MANDATE_HIS:
      actualDataNodes: ds${0..3}.MANDATE_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: MANDATE_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    MEMBER_WORK_HOUR:
      actualDataNodes: ds${0..3}.MEMBER_WORK_HOUR${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: MEMBER_WORK_HOUR${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    MTN_BONUS_CHANGE:
      actualDataNodes: ds${0..3}.MTN_BONUS_CHANGE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: MTN_BONUS_CHANGE${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    MTN_CUST_NOTIFICATION:
      actualDataNodes: ds${0..3}.MTN_CUST_NOTIFICATION${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: MTN_CUST_NOTIFICATION${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    MTN_MENU_SEG:
      actualDataNodes: ds${0..3}.MTN_MENU_SEG${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: MTN_MENU_SEG${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    MTN_PHONE_DEALS_MEMBER:
      actualDataNodes: ds${0..3}.MTN_PHONE_DEALS_MEMBER${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: MTN_PHONE_DEALS_MEMBER${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    MTN_TRANSFER:
      actualDataNodes: ds${0..3}.MTN_TRANSFER${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: MTN_TRANSFER${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    MTN_TRANSFER_ACM:
      actualDataNodes: ds${0..3}.MTN_TRANSFER_ACM${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: MTN_TRANSFER_ACM${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    NEW_NP_DB_HIS:
      actualDataNodes: ds0.NEW_NP_DB_HIS${0..11}
      tableStrategy:
        inline:
          algorithmExpression: NEW_NP_DB_HIS${(PART_ID-1) % 12}
          shardingColumn: PART_ID
    OFFER_PROD_INST_RELA:
      actualDataNodes: ds${0..3}.OFFER_PROD_INST_RELA${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: OFFER_PROD_INST_RELA${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    OFFER_PROD_INST_RELA_HIS:
      actualDataNodes: ds${0..3}.OFFER_PROD_INST_RELA_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: OFFER_PROD_INST_RELA_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    PAYMENT_PROFILE:
      actualDataNodes: ds${0..3}.PAYMENT_PROFILE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: PAYMENT_PROFILE${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    PROD:
      actualDataNodes: ds${0..3}.PROD${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: PROD${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    PROD_ATTR_VALUE:
      actualDataNodes: ds${0..3}.PROD_ATTR_VALUE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: PROD_ATTR_VALUE${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    PROD_ATTR_VALUE_HIS:
      actualDataNodes: ds${0..3}.PROD_ATTR_VALUE_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: PROD_ATTR_VALUE_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    PROD_HIS:
      actualDataNodes: ds${0..3}.PROD_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: PROD_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    PROD_INDIVIDUAL_LIFECYCLE:
      actualDataNodes: ds${0..3}.PROD_INDIVIDUAL_LIFECYCLE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: PROD_INDIVIDUAL_LIFECYCLE${Long.valueOf(CUST_ID) %
            32}
          shardingColumn: CUST_ID
    PROD_STATE_CHANGE_LOG:
      actualDataNodes: ds${0..3}.PROD_STATE_CHANGE_LOG${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: PROD_STATE_CHANGE_LOG${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    PROD_STATE_DETAIL:
      actualDataNodes: ds${0..3}.PROD_STATE_DETAIL${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: PROD_STATE_DETAIL${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SPC_BAL_SHARE:
      actualDataNodes: ds${0..3}.SPC_BAL_SHARE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SPC_BAL_SHARE${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SPC_BAL_SHARE_HIS:
      actualDataNodes: ds0.SPC_BAL_SHARE_HIS${0..11}
      tableStrategy:
        inline:
          algorithmExpression: SPC_BAL_SHARE_HIS${(PART_ID-1) % 12}
          shardingColumn: PART_ID
    SPC_CC_ACCT_HIS:
      actualDataNodes: ds0.SPC_CC_ACCT_HIS${0..11}
      tableStrategy:
        inline:
          algorithmExpression: SPC_CC_ACCT_HIS${(PART_ID-1) % 12}
          shardingColumn: PART_ID
    SPC_CC_SUBS_HIS:
      actualDataNodes: ds0.SPC_CC_SUBS_HIS${0..11}
      tableStrategy:
        inline:
          algorithmExpression: SPC_CC_SUBS_HIS${(PART_ID-1) % 12}
          shardingColumn: PART_ID
    SPC_SUBS_HOME_ZONE_HIS:
      actualDataNodes: ds0.SPC_SUBS_HOME_ZONE_HIS${0..11}
      tableStrategy:
        inline:
          algorithmExpression: SPC_SUBS_HOME_ZONE_HIS${(PART_ID-1) % 12}
          shardingColumn: PART_ID
    SPC_SUBS_UPP_INST_RELA_HIS:
      actualDataNodes: ds0.SPC_SUBS_UPP_INST_RELA_HIS${0..11}
      tableStrategy:
        inline:
          algorithmExpression: SPC_SUBS_UPP_INST_RELA_HIS${(PART_ID-1) % 12}
          shardingColumn: PART_ID
    SPC_USED_RES_HIS:
      actualDataNodes: ds0.SPC_USED_RES_HIS${0..11}
      tableStrategy:
        inline:
          algorithmExpression: SPC_USED_RES_HIS${(PART_ID-1) % 12}
          shardingColumn: PART_ID
    SUBS:
      actualDataNodes: ds${0..3}.SUBS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SUBS_GOODS_INST:
      actualDataNodes: ds${0..3}.SUBS_GOODS_INST${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_GOODS_INST${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SUBS_GOODS_INST_ATTR:
      actualDataNodes: ds${0..3}.SUBS_GOODS_INST_ATTR${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_GOODS_INST_ATTR${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SUBS_GOODS_INST_ATTR_HIS:
      actualDataNodes: ds${0..3}.SUBS_GOODS_INST_ATTR_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_GOODS_INST_ATTR_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SUBS_GOODS_INST_HIS:
      actualDataNodes: ds${0..3}.SUBS_GOODS_INST_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_GOODS_INST_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SUBS_HIS:
      actualDataNodes: ds${0..3}.SUBS_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SUBS_HOME_CELL:
      actualDataNodes: ds${0..3}.SUBS_HOME_CELL${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_HOME_CELL${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SUBS_HOME_ZONE:
      actualDataNodes: ds${0..3}.SUBS_HOME_ZONE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_HOME_ZONE${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SUBS_IDENTIFY:
      actualDataNodes: ds${0..3}.SUBS_IDENTIFY
      databaseStrategy:
        inline:
          algorithmExpression: ds${Math.abs(IDENTIFY_VALUE.hashCode() % 4)}
          shardingColumn: IDENTIFY_VALUE
    SUBS_IDENTIFY_HIS:
      actualDataNodes: ds${0..3}.SUBS_IDENTIFY_HIS
      databaseStrategy:
        inline:
          algorithmExpression: ds${Math.abs(IDENTIFY_VALUE.hashCode() % 4)}
          shardingColumn: IDENTIFY_VALUE
    SUBS_SPECIAL_GROUP:
      actualDataNodes: ds${0..3}.SUBS_SPECIAL_GROUP${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_SPECIAL_GROUP${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SUBS_SPECIAL_GROUP_HIS:
      actualDataNodes: ds${0..3}.SUBS_SPECIAL_GROUP_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_SPECIAL_GROUP_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SUBS_UPP_INST:
      actualDataNodes: ds${0..3}.SUBS_UPP_INST${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_UPP_INST${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SUBS_UPP_INST_HIS:
      actualDataNodes: ds${0..3}.SUBS_UPP_INST_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_UPP_INST_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SUBS_UPP_INST_VALUE:
      actualDataNodes: ds${0..3}.SUBS_UPP_INST_VALUE${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_UPP_INST_VALUE${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    SUBS_UPP_INST_VALUE_HIS:
      actualDataNodes: ds${0..3}.SUBS_UPP_INST_VALUE_HIS${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: SUBS_UPP_INST_VALUE_HIS${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    USED_RES:
      actualDataNodes: ds${0..3}.USED_RES${0..31}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: USED_RES${Long.valueOf(CUST_ID) % 32}
          shardingColumn: CUST_ID
    WF_DISPATCH_ORDER:
      actualDataNodes: ds${0..3}.WF_DISPATCH_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: WF_DISPATCH_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    WF_DISPATCH_ORDER_ACTION:
      actualDataNodes: ds${0..3}.WF_DISPATCH_ORDER_ACTION
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    WF_DISPATCH_ORDER_ACTION_ATTA:
      actualDataNodes: ds${0..3}.WF_DISPATCH_ORDER_ACTION_ATTA
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    WF_DISPATCH_ORDER_ACTION_ATTR:
      actualDataNodes: ds${0..3}.WF_DISPATCH_ORDER_ACTION_ATTR
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    WF_WARN_DETAIL:
      actualDataNodes: ds${0..3}.WF_WARN_DETAIL
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    WF_WARN_LOG:
      actualDataNodes: ds${0..3}.WF_WARN_LOG
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    WF_WORK_ORDER:
      actualDataNodes: ds${0..3}.WF_WORK_ORDER${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: WF_WORK_ORDER${PART_ID % 12}
          shardingColumn: PART_ID
    WF_WORK_ORDER_ACTION:
      actualDataNodes: ds${0..3}.WF_WORK_ORDER_ACTION${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: WF_WORK_ORDER_ACTION${PART_ID % 12}
          shardingColumn: PART_ID
    WF_WORK_ORDER_ACTION_ATTA:
      actualDataNodes: ds${0..3}.WF_WORK_ORDER_ACTION_ATTA
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    WF_WORK_ORDER_ACTION_ATTR:
      actualDataNodes: ds${0..3}.WF_WORK_ORDER_ACTION_ATTR
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
    WF_WORK_ORDER_ATTR:
      actualDataNodes: ds${0..3}.WF_WORK_ORDER_ATTR${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: WF_WORK_ORDER_ATTR${PART_ID % 12}
          shardingColumn: PART_ID
    WF_WORK_ORDER_HIS:
      actualDataNodes: ds${0..3}.WF_WORK_ORDER_HIS${0..11}
      databaseStrategy:
        inline:
          algorithmExpression: ds${ROUTING_ID % 4}
          shardingColumn: ROUTING_ID
      tableStrategy:
        inline:
          algorithmExpression: WF_WORK_ORDER_HIS${PART_ID % 12}
          shardingColumn: PART_ID

